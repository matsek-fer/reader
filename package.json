{
  "name": "matsek-reader",
  "version": "0.1.0",
  "private": true,
  "description": "MatSek Knowledge Forest — vault tooling (slice 1: format + validator)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "js-yaml": "^4.1.0",
    "katex": "^0.18.7",
    "marked": "^18.0.11"
  }
}
